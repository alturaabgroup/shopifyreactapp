# ==============================================
# MODE SELECTION
# ==============================================
# Choose your authentication mode:
# - false = Single-store mode (one Shopify store)
# - true = OAuth mode (multi-merchant installations)
OAUTH_ENABLED=false

# ==============================================
# SINGLE-STORE MODE (OAUTH_ENABLED=false)
# ==============================================
# OPTION 1: Manual configuration (Custom App)
# Get this from: Shopify Admin → Settings → Apps → Your App → API credentials → Admin API access token
SHOPIFY_ADMIN_API_TOKEN=shpat_your_admin_access_token_here
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com

# OPTION 2: Shopify CLI Session (Recommended for CLI apps)
# Leave the above blank and run: shopify app dev
# The CLI will automatically load tokens from ~/.config/shopify/

# ==============================================
# OAUTH MODE (OAUTH_ENABLED=true)
# ==============================================
# Get these from Shopify Partner Dashboard → Your App → Configuration
SHOPIFY_API_KEY=your_api_key_here
SHOPIFY_API_SECRET=your_api_secret_here
SHOPIFY_APP_URL=https://your-app-url.com
SHOPIFY_SCOPES=read_products,write_storefront_access_tokens,read_storefront_access_tokens

# Frontend URL for OAuth redirects (where to send users after authentication)
FRONTEND_URL=http://localhost:3000

# ==============================================
# SHOPIFY API CONFIGURATION
# ==============================================
SHOPIFY_API_VERSION=2025-07

# Storefront Access Token Configuration
# TTL for storefront token cache (in milliseconds)
# Default: 30 days = 2592000000 ms
STOREFRONT_TOKEN_CACHE_TTL=2592000000

# Token rotation policy (in days)
# When to rotate and create a new storefront access token
STOREFRONT_TOKEN_ROTATION_DAYS=30

# ==============================================
# SERVER CONFIGURATION
# ==============================================
PORT=4000
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000

# ==============================================
# PUSH NOTIFICATIONS (Optional)
# ==============================================
# VAPID keys for Web Push
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:your-email@example.com

# Firebase Cloud Messaging (Optional)
FCM_SERVER_KEY=
